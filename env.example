# Environment Configuration
# Possible values: local, staging, production
VITE_ENVIRONMENT=local

# API Configuration
VITE_API_URL=http://localhost:3000/api

# API Base Path (用于 vercel.json/vercel.ts 的 rewrites 配置)
# 如果设置了 VITE_API_PATH，vercel.ts 会优先使用它作为 API 基础 URL
# 如果没有设置，会从 VITE_API_URL 中自动提取基础 URL
# VITE_API_PATH=http://api-staging.screna.ai

# Replace with your actual values for each environment:
# 
# Local Development:
#   VITE_ENVIRONMENT=local
#   VITE_API_URL=http://localhost:3000/api
#   VITE_API_PATH=http://localhost:3000  (可选，用于 vercel.ts)
#
# Staging:
#   VITE_ENVIRONMENT=staging
#   VITE_API_URL=http://screna-staging-screna-server-alb-1701423862.us-east-1.elb.amazonaws.com/api/v1
#   VITE_API_PATH=http://api-staging.screna.ai  (可选，用于 vercel.ts)
#
# Production:
#   VITE_ENVIRONMENT=production
#   VITE_API_URL=http://your-production-api-url.com/api/v1
#   VITE_API_PATH=http://api-production.screna.ai  (可选，用于 vercel.ts)
